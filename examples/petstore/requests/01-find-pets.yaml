name: find pets by status
protocol: http
http:
  method: GET
  url: "{{base}}/pet/findByStatus"
  query:
    status: available
assertions:
  - name: returns an array
    from: body
    path: 0.id
    op: exists
