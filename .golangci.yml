version: "2"
linters:
  default: standard          # errcheck, govet, staticcheck, ineffassign, unused
  enable:
    - revive
    - bodyclose              # an HTTP client repo must lint for leaked response bodies
    - errorlint
    - noctx
formatters:
  enable:
    - gofmt
    - goimports
