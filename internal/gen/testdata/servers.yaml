openapi: 3.1.0
info:
  title: Servers
  version: "1.0"
servers:
  - url: https://api.example.com/v1
  - url: https://{region}.staging.example.com/v1
    variables:
      region:
        default: eu
paths:
  /ping:
    get:
      operationId: ping
      responses:
        "200":
          description: ok
